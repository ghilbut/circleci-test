version: 2.1

executors:
  default:
    docker:
      - image: cimg/base:stable

jobs:
  default:
    executor: default
    steps:
      - checkout
      - run: echo "base job!!"

workflows:
  default:
    jobs:
      - default
